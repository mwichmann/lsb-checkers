// Generated by gen_lib.pl

#include "../../tests/type_tests.h"
#include "../../misc/lsb_output.h"
#include "stdlib.h"
#include <nspr4/prnetdb.h>
#undef PR_FreeAddrInfo
static void(*funcptr) (PRAddrInfo * ) = 0;

extern int __lsb_check_params;
void PR_FreeAddrInfo (PRAddrInfo * arg0 )
{
	int reset_flag = __lsb_check_params;
	__lsb_output(4, "Invoking wrapper for PR_FreeAddrInfo()");
	if(!funcptr)
		funcptr = dlsym(RTLD_NEXT, "PR_FreeAddrInfo");
	if(!funcptr) {
		__lsb_output(-1, "Failed to load PR_FreeAddrInfo. Probably the library was loaded using dlopen, we don't support this at the moment.");
		exit(1);
	}
	if(__lsb_check_params)
	{
		__lsb_check_params=0;
		__lsb_output(4, "PR_FreeAddrInfo() - validating");
		if( arg0 ) {
		validate_RWaddress( arg0, "PR_FreeAddrInfo - arg0 (addrInfo)");
		}
		validate_NULL_TYPETYPE(  arg0, "PR_FreeAddrInfo - arg0 (addrInfo)");
	}
	funcptr(arg0);
	__lsb_check_params = reset_flag;
}


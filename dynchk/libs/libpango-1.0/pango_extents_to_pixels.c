// Generated by gen_lib.pl

#include "../../tests/type_tests.h"
#include "../../misc/lsb_output.h"
#include "stdlib.h"
#include <pango-1.0/pango/pango.h>
#undef pango_extents_to_pixels
static void(*funcptr) (PangoRectangle * , PangoRectangle * ) = 0;

extern int __lsb_check_params;
void pango_extents_to_pixels (PangoRectangle * arg0 , PangoRectangle * arg1 )
{
	int reset_flag = __lsb_check_params;
	__lsb_output(4, "Invoking wrapper for pango_extents_to_pixels()");
	if(!funcptr)
		funcptr = dlsym(RTLD_NEXT, "pango_extents_to_pixels");
	if(!funcptr) {
		__lsb_output(-1, "Failed to load pango_extents_to_pixels. Probably the library was loaded using dlopen, we don't support this at the moment.");
		exit(1);
	}
	if(__lsb_check_params)
	{
		__lsb_check_params=0;
		__lsb_output(4, "pango_extents_to_pixels() - validating");
		if( arg0 ) {
		validate_RWaddress( arg0, "pango_extents_to_pixels - arg0 (inclusive)");
		}
		validate_NULL_TYPETYPE(  arg0, "pango_extents_to_pixels - arg0 (inclusive)");
		if( arg1 ) {
		validate_RWaddress( arg1, "pango_extents_to_pixels - arg1 (nearest)");
		}
		validate_NULL_TYPETYPE(  arg1, "pango_extents_to_pixels - arg1 (nearest)");
	}
	funcptr(arg0, arg1);
	__lsb_check_params = reset_flag;
}


// Generated by gen_lib.pl

#include "../../tests/type_tests.h"
#include "../../misc/lsb_output.h"
#include "stdlib.h"
#include <pango-1.0/pango/pango-utils.h>
#undef pango_quantize_line_geometry
static void(*funcptr) (int * , int * ) = 0;

extern int __lsb_check_params;
void pango_quantize_line_geometry (int * arg0 , int * arg1 )
{
	int reset_flag = __lsb_check_params;
	__lsb_output(4, "Invoking wrapper for pango_quantize_line_geometry()");
	if(!funcptr)
		funcptr = dlsym(RTLD_NEXT, "pango_quantize_line_geometry");
	if(!funcptr) {
		__lsb_output(-1, "Failed to load pango_quantize_line_geometry. Probably the library was loaded using dlopen, we don't support this at the moment.");
		exit(1);
	}
	if(__lsb_check_params)
	{
		__lsb_check_params=0;
		__lsb_output(4, "pango_quantize_line_geometry() - validating");
		if( arg0 ) {
		validate_RWaddress( arg0, "pango_quantize_line_geometry - arg0 (thickness)");
		}
		validate_NULL_TYPETYPE(  arg0, "pango_quantize_line_geometry - arg0 (thickness)");
		if( arg1 ) {
		validate_RWaddress( arg1, "pango_quantize_line_geometry - arg1 (position)");
		}
		validate_NULL_TYPETYPE(  arg1, "pango_quantize_line_geometry - arg1 (position)");
	}
	funcptr(arg0, arg1);
	__lsb_check_params = reset_flag;
}


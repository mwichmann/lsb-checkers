#include <stdio.h>
#include <sys/types.h>
#include "../libs/__lsb_funcs.h"

int validate_pathname(const char *arg, const char *name)
{
	//__lsb_fprintf(stderr, "lsbdynchk: %s: %s is a parameter.(stub)\n", name, arg);
	return 0;
}

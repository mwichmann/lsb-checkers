#!/usr/bin/perl

use CGI;
use Mysql;

use Env qw(LSBUSER LSBDBPASSWD LSBDB LSBDBHOST);

$Dbh = Mysql->connect($LSBDBHOST,$LSBDB,$LSBUSER, $LSBDBPASSWD) || die $Mysql::db_errstr;

$select = "SELECT DISTINCT * FROM Library ";
$select.= "LEFT JOIN Architecture ON Aid=Larch ";
$select.= "WHERE Library.Lstd='Yes' ";
$select.= "ORDER BY Lrunname ";

#print $select;

$sth = $Dbh->query($select) || die $Dbh->errmsg();

print "char *DtNeeded[] = {\n";
for(1..$sth->numrows) {
	%entry=$sth->fetchhash;
	if( $entry{'Aname'} ne "All" ) {
                print "#if defined(".$entry{'Asymbol'}.")\n";
        }
	printf "\t\"%s\",\n",$entry{'Lrunname'};
	if( $entry{'Aname'} ne "All" ) {
                print "#endif /* ".$entry{'Asymbol'}." */\n";
        }
}
print "\t};\n";
print "int numDtNeeded = sizeof(DtNeeded)/sizeof(char *);\n";

